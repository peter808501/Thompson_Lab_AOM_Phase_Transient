"circuit.sp" is the initial spice file we began with, before any optimization. 
"corrected.sp" is the final spice file after all optimizations. 
"spice_optimization" is the Jupyter notebook in which the optimization is done. 